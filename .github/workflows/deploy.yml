name: Deploy Portal via SSM

on:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure AWS credentials via OIDC
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::476114140163:role/GitHub-Portal-DeployRole
          aws-region: us-east-1

      - name: Deploy using SSM
        run: |
          aws ssm send-command \
            --instance-ids "i-05e4578276976506e" \
            --document-name "AWS-RunShellScript" \
            --comment "Deploy portal from GitHub" \
            --parameters commands='[
              "cd /opt/dev-portal-next",
              "git pull origin main",
              "sudo chown -R ec2-user:ec2-user /opt/dev-portal-next",
              "sudo -u ec2-user /usr/bin/pnpm install --frozen-lockfile",
              "sudo -u ec2-user /usr/bin/pnpm build",
              "sudo -u ec2-user /usr/bin/pm2 delete dev-portal-sismas || true",
              "sudo -u ec2-user /usr/bin/pm2 start pnpm --name dev-portal-sismas --cwd /opt/dev-portal-next -- start",
              "sudo -u ec2-user /usr/bin/pm2 save",
              "sudo -u ec2-user /usr/bin/pm2 status"
            ]'
